{
  "name": "torbox-streamer",
  "version": "2.1.1",
  "description": "TorBox Streamer browser extension for Firefox & Waterfox",
  "main": "extension/background.js",
  "scripts": {
    "build": "python3 package.py",
    "lint": "eslint extension tests playwright.config.js vitest.config.mjs",
    "test:unit": "vitest run tests/unit",
    "test:unit:coverage": "vitest run tests/unit --coverage",
    "test:unit:watch": "vitest tests/unit",
    "test:e2e:mock": "npm run build && playwright test tests/e2e/mock tests/e2e/extension",
    "test:e2e:native": "python3 -m unittest discover -s tests/e2e -p \"test_*.py\"",
    "test:e2e:online": "npm run build && playwright test --config playwright.online.config.js",
    "test:e2e": "npm run test:e2e:mock && npm run test:e2e:native",
    "test": "npm run lint && npm run test:unit:coverage && npm run test:e2e"
  },
  "keywords": [
    "torbox",
    "browser-extension",
    "firefox",
    "stremio",
    "torrentio"
  ],
  "author": "flamprakis",
  "license": "MIT",
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@vitest/coverage-v8": "^4.1.9",
    "eslint": "^10.9.1",
    "globals": "^17.12.0",
    "vitest": "^4.1.9"
  }
}
